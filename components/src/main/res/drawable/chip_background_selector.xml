<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Disabled -->
    <item android:color="@color/surfaceDisabled" android:state_enabled="false" />

    <!-- Selected -->
    <item android:color="@color/zebra_blue_solid_selected" android:state_checked="true" />

    <!-- Unselected -->
    <item android:color="@color/background" />
</selector>
