<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Disabled -->
    <item android:color="@color/zebra_grey_subtle_stroke" android:state_enabled="false" />

    <!-- Selected -->
    <item android:color="@color/zebra_blue_enabled" android:state_checked="true" />

    <!-- Unselected -->
    <item android:color="@color/zebra_grey_subtle_stroke" />
</selector>
